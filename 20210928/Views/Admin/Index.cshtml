@model IEnumerable<Vapor.Areas.Identity.Data.User>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @*{//Html.DisplayNameFor(model => model.)}*@
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    
                </td>

            </tr>
        }
    </tbody>
</table>

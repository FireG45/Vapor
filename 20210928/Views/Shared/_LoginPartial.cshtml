@using Microsoft.AspNetCore.Identity
@using Vapor.Areas.Identity.Data

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

<ol class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            @*<a id="manage" class="nav-link text-light" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                    <i class="fa fa-user-circle" aria-hidden="true"></i>

                    @UserManager.GetUserName(User)

                </a>*@
            <div class="dropdown">



                <a class="dropleft text-light" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-user-circle" aria-hidden="true"></i>
                    @UserManager.GetUserName(User)
                </a>

                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">

                    <a id="manage" class="dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Аккаунт</a>

                    <a id="logoutForm" class="dropdown-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                        @*<button id="logout" type="submit" class="nav-link btn text-dark">Выйти</button>*@
                        Выйти
                    </a>

                    <a class="dropdown-item" asp-controller="Items" asp-action="ShopCart">Корзина</a>
                </div>

            </div>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text" id="register" asp-area="Identity" asp-page="/Account/Register">Зарегистрироваться</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text" id="login" asp-area="Identity" asp-page="/Account/Login">Войти</a>
        </li>
    }
</ol>

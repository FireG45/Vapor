@model IEnumerable<Item>
@{ CultureInfo.CurrentCulture = new CultureInfo("en-US"); CultureInfo.CurrentUICulture = new CultureInfo("en-US"); }
@inject IHtmlLocalizer<SharedResource> Localizer

@{
    ViewData["Title"] = Localizer["Wishlist"];
}


<h2>Желаемое</h2>
<table class="table-borderless">

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div class="container  text-white">

                        <div class="row">
                            <partial name="OneItemWishList" model="item" />
                        </div>

                    </div>
                </td>
            </tr>
        }

    </tbody>
</table>



@*<h4 class="text-right">
        Сумма : @sum.ToString("c")<br>
        <a class=" btn btn-outline-danger" asp-controller="Items" asp-action="Summary">Оплатить</a>
    </h4>*@
